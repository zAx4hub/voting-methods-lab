# Benchmarks — voting-methods-lab

Author: zAx4hub

| Scenario | Notes |
|---|---|
| demo payload | `npm run demo` |
| sample-input | `npm start -- run examples/sample-input.json` |
| tests | `npm test` |

Compare against ad-hoc scripts: this repo ships a deterministic engine + CI.
